#include <iostream>
#include <string>
#include <vector>
#include <algorithm> // sort 사용
using namespace std;

int main() {
    string S;
    cin >> S;
    int i;
    cin >> i;
    cout << S[i - 1] << endl;
    return 0;
  }

